% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_multinets.R
\name{compare_multinets}
\alias{compare_multinets}
\title{Compare two \code{ml.network} objects and \strong{print the differences}}
\usage{
compare_multinets(net_a, net_b, tol = 1e-09, show_max = 10, verbose = TRUE)
}
\arguments{
\item{net_a, net_b}{Two \code{multinet::ml.network} objects to compare.}

\item{tol}{Numeric tolerance for weight comparison. Default \code{1e-9}.}

\item{show_max}{Maximum number of mismatched items to print per category
(actors / edges / weights) for each layer. Default \code{10}.}

\item{verbose}{Logical; if \code{TRUE}, prints a summary and per-layer differences.
Default \code{TRUE}.}
}
\value{
(Invisibly) a list with:
\itemize{
\item \code{ok}: logical, \code{TRUE} if fully equivalent, else \code{FALSE}
\item \code{layers_all}: character vector of all layer names considered
\item \code{missing_in_a}, \code{missing_in_b}: layers missing from A or B
\item \code{per_layer}: named list; for each layer:
\itemize{
\item \code{ok_layer}: logical; \code{TRUE} if that layer matches
\item \code{directed_mismatch}: logical
\item \code{actors_only_a}, \code{actors_only_b}: character vectors
\item \code{edges_only_a}, \code{edges_only_b}: character vectors of edge keys
\item \code{weight_mismatches}: data.frame with \code{key}, \code{wA}, \code{wB}, \code{delta}
}
}
}
\description{
Uses multinet's \code{as.list()} to obtain one \code{igraph} per layer and then checks
that both multilayer networks have the same layers, the same \strong{actor sets},
and the same \strong{edge sets/weights} (to a tolerance). When a mismatch is
found, the function \strong{prints a human-readable diff} (which layers differ,
how they differ, and sample items).
}
\details{
Differences reported per layer:
\itemize{
\item \strong{Directedness} mismatch (one directed, the other undirected)
\item \strong{Actors only in A} / \strong{only in B} (vertex \code{name})
\item \strong{Edges only in A} / \strong{only in B} (canonical undirected keys \code{u||v} or
directed keys \code{u->v})
\item \strong{Weight mismatches} on common edges beyond \code{tol} (shows \code{wA}, \code{wB}, \code{Δ})
}
}
\examples{
\dontrun{
# Assuming you've already built both multilayer networks:
# net_edges <- build_multiNet(...); net_graphs <- build_multinet_from_graphs(...)

diff <- compare_multinets(
  net_a   = net_edges,
  net_b   = net_graphs,
  tol     = 1e-9,
  show_max= 10,
  verbose = TRUE
)

if (!diff$ok) {
  # Programmatic access to details:
  names(diff$per_layer)                # layers compared
  diff$per_layer[["E1"]]$edges_only_a  # edges only in A for layer E1
}
}
}
