% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_multilayer_network.R
\name{create_multilayer_network}
\alias{create_multilayer_network}
\title{Create a Multi-layer Network from Selected Edges}
\usage{
create_multilayer_network(selected_edges, genes_info)
}
\arguments{
\item{selected_edges}{A named list of data frames, where each element represents
the edge list of a specific group (e.g., age group or condition).
Each data frame must contain columns: \code{from}, \code{to}, and \code{weight}.}

\item{genes_info}{A data frame containing gene annotation information.
It must contain two columns: \code{GeneName} and \code{GeneType}.}
}
\value{
An object of class \code{ml.network} representing the constructed multilayer network.
}
\description{
Constructs a multilayer network using the \code{multinet} package by converting
selected edge lists into individual layer graphs, assigning node-level
attributes (e.g., gene types), and adding each layer to the network.
}
\details{
\itemize{
\item Converts each edge list to an \code{igraph} object.
\item Assigns \code{GeneType} as a node attribute.
\item Constructs a multilayer network using \code{multinet}.
\item Prints the number of genes per layer and returns the multilayer network.
}
}
\examples{
\dontrun{
net <- create_multilayer_network(selected_edges, genes_info)
summary(net)
}

}
